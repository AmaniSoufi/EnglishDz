# دليل الربط بين الفرونت إند والباك إند

تم ربط تطبيق EnglishDz مع الباك إند بنجاح! 🎉

## ما تم إنجازه:

1. ✅ إضافة dependencies المطلوبة في `pubspec.yaml`:
   - `quiz_backend_client`
   - `serverpod_flutter`
   - `serverpod_auth_idp_flutter`

2. ✅ إنشاء `lib/config/app_config.dart` لتهيئة Client

3. ✅ إنشاء `lib/services/quiz_service.dart` لتحويل البيانات من الباك إند إلى نموذج الفرونت إند

4. ✅ تحديث `lib/bloc/blocimplementation.dart` لجلب الأسئلة من الباك إند بدلاً من البيانات المثبتة

5. ✅ تحديث `lib/main.dart` لتهيئة Client عند بدء التطبيق

## الخطوات التالية:

### 1. تثبيت Dependencies:

```bash
cd englishDz/englishdz
flutter pub get
```

**ملاحظة مهمة**: إذا واجهت خطأ في `serverpod_auth_idp_flutter`، جرب استخدام النسخة المناسبة:

```yaml
dependencies:
  serverpod_flutter: ^3.0.0
  serverpod_auth_idp_flutter: ^3.0.0
```

أو بدون auth (إذا لم تكن تحتاج المصادقة):

```yaml
dependencies:
  serverpod_flutter: ^3.0.0
```

### 2. التأكد من أن الباك إند يعمل:

```bash
# في مجلد quiz_backend_server
dart bin/main.dart --apply-migrations
```

يجب أن يعمل السيرفر على `http://localhost:8080`

### 3. تحديث عنوان السيرفر (إذا لزم الأمر):

في ملف `lib/config/app_config.dart`، يمكنك تغيير عنوان السيرفر:

```dart
// للتطوير المحلي
const serverUrl = 'http://localhost:8080';

// للأجهزة الفعلية (استبدل YOUR_IP بالعنوان الفعلي)
const serverUrl = 'http://YOUR_IP:8080';

// للإنتاج
const serverUrl = 'https://your-api-domain.com';
```

### 4. تشغيل التطبيق:

```bash
flutter run
```

## كيفية عمل النظام:

1. **عند بدء التطبيق**: يتم تهيئة `quizClient` في `main.dart`

2. **عند اختيار مستوى**: يتم استدعاء `LoadQuestions(level)` event

3. **في QuizBloc**: يتم استدعاء `QuizService.getQuestionsByLevel(level)`

4. **في QuizService**: 
   - يتم الاتصال بالباك إند عبر `client.questions.getByLevel(level)`
   - يتم تحويل `Question` من الباك إند إلى `Question` في الفرونت إند

5. **العرض**: يتم عرض الأسئلة في QuizScreen

## هيكل البيانات:

### في الباك إند:
```dart
Question {
  questionText: String
  correctAnswer: String
  option1, option2, option3, option4: String
  level: String
}
```

### في الفرونت إند:
```dart
Question {
  question: String
  correct: String
  answers: List<String>
}
```

التحويل يتم تلقائياً في `QuizService._convertToFrontendQuestion()`

## استكشاف الأخطاء:

### خطأ: "Connection refused"
- تأكد من أن الباك إند يعمل
- تحقق من عنوان السيرفر في `app_config.dart`

### خطأ: "Package not found"
- قم بتشغيل `flutter pub get`
- تأكد من أن المسار إلى `quiz_backend_client` صحيح في `pubspec.yaml`

### خطأ: "No questions found"
- تأكد من وجود أسئلة في قاعدة البيانات للمستوى المحدد
- تأكد من أن migrations تم تطبيقها على قاعدة البيانات

### خطأ في serverpod_auth_idp_flutter
- إذا لم تكن تحتاج المصادقة، يمكنك إزالة dependency من `pubspec.yaml`
- أو استخدم نسخة متوافقة مع serverpod_flutter

## ملاحظات:

- الأسئلة الآن تُجلب من قاعدة البيانات في الباك إند
- كل مستوى (BAC, BEM, A1, A2, B1, B2, C1, C2) يمكن أن يحتوي على أسئلة مختلفة
- يمكنك إضافة أسئلة جديدة عبر قاعدة البيانات أو من خلال الباك إند

## الخطوات المستقبلية (اختياري):

1. إضافة إدارة الأخطاء بشكل أفضل (error handling)
2. إضافة loading states محسّنة
3. إضافة caching للأسئلة
4. إضافة إحصائيات النتائج

